#pragma once

#include <cstdint>

namespace EWAN
{
    struct Settings
    {
        std::uint16_t WindowWidth = 0;
        std::uint16_t WindowHeight = 0;
        std::uint16_t WindowLeft = 0;
        std::uint16_t WindowTop = 0;
    };
}
